# ============================================
# Production Environment Variables Template
# ============================================
# Copy this file to .env in the project root and fill in your actual values
# DO NOT commit .env to version control

# --------------------------------------------
# Node Environment
# --------------------------------------------
NODE_ENV=production

# --------------------------------------------
# Application
# --------------------------------------------
PORT=8000
APP_NAME="Quantum Sport"
APP_URL=https://api.quantumsocialclub.id
BASE_URL=https://api.quantumsocialclub.id

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# IMPORTANT: Fill these values before deploying
DB_NAME=quantum_sport
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
# Tip: wrap passwords with special characters ($, !, spaces) in quotes:
# DB_PASSWORD='a1ndi$'
DB_HOST=db
DB_PORT=5432

# Full database URL (auto-constructed in docker-compose)
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public&connection_limit=10&pool_timeout=20

# --------------------------------------------
# Redis Configuration
# --------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}

# --------------------------------------------
# JWT & Security
# --------------------------------------------
JWT_SECRET=your_jwt_secret_min_32_chars_here
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_refresh_secret_min_32_chars_here
JWT_REFRESH_EXPIRES_IN=30d

# --------------------------------------------
# Email Configuration (SMTP)
# --------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@your-domain.com

# --------------------------------------------
# Payment Gateway (Xendit)
# --------------------------------------------
XENDIT_SECRET_KEY=your_xendit_secret_key
XENDIT_WEBHOOK_TOKEN=your_xendit_webhook_token
XENDIT_PUBLIC_KEY=your_xendit_public_key

# --------------------------------------------
# File Storage (Vercel Blob)
# --------------------------------------------
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token

# Database backups (uploaded to Vercel Blob under BLOB_BACKUP_PREFIX)
BLOB_BACKUP_PREFIX=quantum-sport/db
BACKUP_RETENTION_DAYS=30
# BACKUP_CRON_LOG=/path/to/project/logs/backup.log
# BACKUP_CRON_SCHEDULE=30 2 * * *

# --------------------------------------------
# Frontend URL (for CORS)
# --------------------------------------------
FRONTEND_URL=https://quantumsocialclub.id
# Add more frontend URLs if needed (comma-separated)
# FRONTEND_URL=https://quantumsocialclub.id,https://www.quantumsocialclub.id

# --------------------------------------------
# SSL Configuration (for Let's Encrypt)
# --------------------------------------------
SSL_EMAIL=admin@quantumsocialclub.id

# --------------------------------------------
# Optional: Monitoring & Logging
# --------------------------------------------
# SENTRY_DSN=your_sentry_dsn
# LOG_LEVEL=info

# --------------------------------------------
# Optional: SMS/Phone Verification
# --------------------------------------------
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=your_twilio_phone_number

